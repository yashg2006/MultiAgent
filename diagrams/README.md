# Diagrams Directory

This directory stores architecture, sequence, and UML diagrams.

## Organization Structure
- `/src_diagrams`: Editable source files (e.g. `.drawio`, `.puml`, `.mermaid`).
- `/exports`: Rendered/exported image assets (`.png`, `.svg`, `.pdf`) for inclusion in documentation and reports.
